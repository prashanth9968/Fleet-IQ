name: fleetiq_widgets
description: UI Component package for FleetIQ mobile apps containing StatusBadges, MetricGauges, CustomButtons, and AlertRows.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  fleetiq_theme:
    path: ../theme
  fleetiq_models:
    path: ../models
