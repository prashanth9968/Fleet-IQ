name: fleetiq_models
description: Data transfer objects (DTO) for FleetIQ applications (Vehicle, Trip, DriverScore, AlertHistory).
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
