apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleetiq-tracking-service
  labels:
    app: fleetiq-tracking-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fleetiq-tracking-service
  template:
    metadata:
      labels:
        app: fleetiq-tracking-service
    spec:
      containers:
      - name: tracking-service
        image: fleetiq/fleetiq-tracking-service:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: fleetiq-config
        - secretRef:
            name: fleetiq-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
spec:
  selector:
    app: fleetiq-tracking-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
