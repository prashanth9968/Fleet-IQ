apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleetiq-alerts-service
  labels:
    app: fleetiq-alerts-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fleetiq-alerts-service
  template:
    metadata:
      labels:
        app: fleetiq-alerts-service
    spec:
      containers:
      - name: alerts-service
        image: fleetiq/fleetiq-alerts-service:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: fleetiq-config
        - secretRef:
            name: fleetiq-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: alerts-service
spec:
  selector:
    app: fleetiq-alerts-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
